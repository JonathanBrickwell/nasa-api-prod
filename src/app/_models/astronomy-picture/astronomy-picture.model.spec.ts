import { AstronomyPicture } from './astronomy-picture.model';

describe('AstronomyPicture', () => {
  it('should create an instance', () => {
    expect(new AstronomyPicture()).toBeTruthy();
  });
});
